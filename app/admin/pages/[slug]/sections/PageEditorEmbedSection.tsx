// Retired in plan 008 (embed kit removal carve-out: the CORS allowlist moved to the
// Page section). Safe to `git rm`; physical deletion was permission-blocked in the
// executor sandbox.
export {};
