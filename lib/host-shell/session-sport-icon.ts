/**
 * @deprecated Portal cards use HostPortalSportIcon (SVG assets + built-in silhouettes).
 * Kept for any legacy imports; re-exports normalize helper only.
 */
export { normalizeSportKey, getSportIconAssetPath } from '@/lib/host-shell/sport-visuals';
